// Settings endpoints for SLA and workflow configuration views.
const settingsApi = {
  SLA: "/settings/sla",
  PRIORITIES: "/settings/priorities",
  WORKFLOW: "/settings/workflow",
};

export default settingsApi;
